export const SubTitle = ({ children }) => (
  <h2>
    {children}

    <style jsx>
      {`
        font-size: 16px;
        font-weight: 600;
        color: #949494;
        text-transform: uppercase;
        margin: 0 0 5px;
      `}
    </style>
  </h2>
);
