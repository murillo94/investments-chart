export const Title = ({ children }) => (
  <h1>
    {children}

    <style jsx>
      {`
        font-size: 16px;
        color: #949494;
        text-transform: uppercase;
        margin: 0 0 5px;
      `}
    </style>
  </h1>
);
